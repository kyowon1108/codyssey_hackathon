# FastAPI and web server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.0

# Database
sqlalchemy>=2.0.0

# Scientific computing and ML
numpy>=1.24.0
scikit-learn>=1.3.0

# 3D file handling
plyfile>=0.8.1

# Image processing
pillow>=10.0.0

# Utilities
tqdm>=4.65.0

# PyTorch and related packages (CUDA 12.8 compatible)
# Install separately with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# Note: COLMAP and Gaussian Splatting dependencies should be installed separately
# - COLMAP: sudo apt-get install colmap
# - Gaussian Splatting: See README for setup instructions
